gcloud container clusters get-credentials mycluster
gcloud container clusters create mycluster
gcloud compute disks create --size 1GB main-disk
run kubectl create -f 01-05*yaml files
clusterIP should be set before creating pv.yaml file.

used https://medium.com/platformer-blog/nfs-persistent-volumes-with-kubernetes-a-case-study-ce1ed6e2c266

kubectl create secret generic db-user-pass --from-file=./username --from-file=./password --from-file=./db --from-file=./host
kubectl create secret generic cloudsql-instance-credentials --from-file=~/Downloads/metal-imprint-238115-a4c6ab0d2cf9.json
kubectl create secret generic sendgrid-api-key -from-file=./api_key

